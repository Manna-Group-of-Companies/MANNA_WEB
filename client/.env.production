# Used only by `npm run build` (vite build). The API is on a different origin
# than the static site in production, so requests need the absolute URL.
# No trailing slash — api.ts concatenates paths like `/api/leads` directly.
VITE_API_BASE_URL=https://manna-web-4qef.onrender.com

VITE_GOOGLE_CLIENT_ID=127207137422-1380j29k4ru6d6uh4ujf6g7qnpu19but.apps.googleusercontent.com
