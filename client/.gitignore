node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
# The production API origin is public information, and committing it keeps
# host-side builds working without extra dashboard config.
!.env.production
.vite
